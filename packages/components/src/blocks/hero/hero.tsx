/**
 * Hero Block
 * Auto-block created by buildHeroBlock() in scripts.js from an h1 + picture pair.
 * Decoration is handled entirely by hero.css — no JS transformation needed.
 */
export default function decorate(_block: HTMLElement): void {
  // intentionally empty — styling is CSS-only
}

